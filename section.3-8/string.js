"use strict";

var
  firstName = 'John',
  lastName = 'Doe',
  containsString = ' number of characters.';

console.log(firstName + ' ' + lastName);
console.log(firstName + ' contains ' + firstName.length + containsString);
console.log(lastName + ' contains ' + lastName.length + containsString);
